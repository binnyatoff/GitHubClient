<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/git_nav"
    app:startDestination="@id/Users">

    <fragment
        android:id="@+id/Users"
        android:name="ru.binnyatoff.githubclient.screens.feed.Users"
        android:label="fragment_users"
        tools:layout="@layout/fragment_users">
        <action
            android:id="@+id/action_Users_to_UserDetail"
            app:destination="@id/UserDetail" />
    </fragment>

    <fragment
        android:id="@+id/UserDetail"
        android:name="ru.binnyatoff.githubclient.screens.UserDetails"
        android:label="fragment_user"
        tools:layout="@layout/fragment_user_details">
    </fragment>
</navigation>